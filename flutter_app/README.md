# flutter_app

Geo Journal

Aplikacja z natywną funkcją (lokalizacja GPS) oraz komunikacja z API (GET, POST),
zawierająca 3 widoki - List of Entries, Add Entry (dialog), Settings.


1) python3 server.py
2) flutter run